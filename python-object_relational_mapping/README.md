# Python - Object Relational Mapping

This project links Python and MySQL using `MySQLdb` and SQLAlchemy ORM.

Files follow Holberton project requirements for:
- MySQLdb tasks (`0` to `5`)
- SQLAlchemy model/scripts (`6` to `14`)
